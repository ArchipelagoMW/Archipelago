

class DifficultyAnalysis(object):

    def __init__(self):

    def difficulty_with_settings(self, settings_flags):
        new_preprocessed = PreprocessedData(settings_flags)

        self.allocation = Allocation(data, preprocessed