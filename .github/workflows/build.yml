# This workflow will build a release-like distribution when manually dispatched:
# a Windows x64 7zip, a Windows x64 Installer, a Linux AppImage and a Linux binary .tar.gz.

name: Build

on:
  push:
    paths:
      - '.github/workflows/build.yml'
      - 'setup.py'
      - 'requirements.txt'
      - '*.iss'
      - 'worlds/*/archipelago.json'
  pull_request:
    paths:
      - '.github/workflows/build.yml'
      - 'setup.py'
      - 'requirements.txt'
      - '*.iss'
      - 'worlds/*/archipelago.json'
  workflow_dispatch:

env:
  ENEMIZER_VERSION: 7.1
  # NOTE: since appimage/appimagetool and appimage/type2-runtime does not have tags anymore,
  #       we check the sha256 and require manual intervention if it was updated.
  APPIMAGE_FORK: 'PopTracker'
  APPIMAGETOOL_VERSION: 'r-2025-11-18'
  APPIMAGETOOL_X86_64_HASH: '4577a452b30af2337123fbb383aea154b618e51ad5448c3b62085cbbbfbfd9a2'
  APPIMAGE_RUNTIME_VERSION: 'r-2025-11-07'
  APPIMAGE_RUNTIME_X86_64_HASH: '27ddd3f78e483fc5f7856e413d7c17092917f8c35bfe3318a0d378aa9435ad17'

permissions:  # permissions required for attestation
  id-token: 'write'
  attestations: 'write'

jobs:
  build-release-macos:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - macos-latest    # arm64 (Apple Silicon)
          - macos-15-intel  # x86_64
    steps:
      # - copy code below to release.yml -
      - uses: actions/checkout@v6.0.2
      - name: Install python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: '~3.12.7'
          check-latest: true
      - name: Install coreutils  # provides GNU `timeout` for the validation steps below (not present on macOS)
        run: |
          brew install coreutils
          echo "$(brew --prefix)/opt/coreutils/libexec/gnubin" >> "$GITHUB_PATH"
      - name: Build
        run: |
          python -m pip install --upgrade pip
          # build_exe --yes bootstraps the build deps, bdist_mac reuses that build to assemble the .app
          python setup.py build_exe --yes bdist_mac
          if ( ! ls build/*.app >/dev/null 2>&1 ); then
            echo "No .app found in build/"
            exit 1
          fi
          APP_PATH="$(ls -1dt build/*.app | head -n1)"
          echo "Found $APP_PATH"
          echo "APP_PATH=$APP_PATH" >> $GITHUB_ENV
          # name the DMG like the AppImage: Archipelago_<version>_<arch>
          VERSION="$(python -c 'import Utils; print(Utils.__version__)')"
          ARCH="$(python -c 'import platform; print(platform.machine())')"
          echo "DMG_BASE=Archipelago_${VERSION}_macos-${ARCH}" >> $GITHUB_ENV
      - name: Ad-hoc sign app
        run: |
          codesign --force --deep --sign - "$APP_PATH"
          codesign --verify --deep --strict --verbose=2 "$APP_PATH"
      - name: Create DMG
        run: |
          brew install create-dmg
          sudo mdutil -a -i off || true
          APP_NAME="$(basename "$APP_PATH")"
          cat > bg.svg <<'EOF'
          <svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="660" height="400" viewBox="0 0 660 400" fill="none">
          <path fill="#F1F1F5" d="M0 400L660 400L660 0L0 0L0 400Z"></path>
          <path fill-rule="evenodd" fill="#4E5969" d="M343.933 177.984L321.661 159.275C320.439 158.249 318.656 158.249 317.434 159.275L317.066 159.584C315.503 160.897 315.503 163.303 317.066 164.616L332.981 177.984C334.544 179.297 334.544 181.703 332.981 183.016L317.067 196.384C315.503 197.697 315.503 200.103 317.067 201.416L317.434 201.725C318.656 202.751 320.439 202.751 321.661 201.725L343.933 183.016C345.497 181.703 345.497 179.297 343.933 177.984Z"></path>
          </svg>
          EOF
          for attempt in 1 2 3; do
            if create-dmg \
              --volname "$DMG_BASE" \
              --background bg.svg \
              --window-pos 200 120 \
              --window-size 660 400 \
              --icon-size 100 \
              --icon "$APP_NAME" 160 185 \
              --app-drop-link 500 185 \
              --no-internet-enable \
              "${DMG_BASE}.dmg" "$APP_PATH"; then
              break
            fi
            echo "create-dmg attempt $attempt failed; cleaning up before retry"
            for vol in /Volumes/dmg.*; do hdiutil detach "$vol" -force 2>/dev/null || true; done
            rm -f "${DMG_BASE}.dmg" rw.*.dmg
            if [ "$attempt" -eq 3 ]; then exit 1; fi
          done
          echo "DMG_NAME=${DMG_BASE}.dmg" >> $GITHUB_ENV
      # - copy code above to release.yml -
      - name: Attest Build
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/attest@v4.1.0
        with:
          subject-path: |
            build/*.app/Contents/MacOS/ArchipelagoLauncher
            build/*.app/Contents/MacOS/ArchipelagoGenerate
            build/*.app/Contents/MacOS/ArchipelagoServer
            ${{ env.DMG_NAME }}
      - name: Check build loads expected worlds
        run: |
          # A frozen macOS app keeps user files (Players/, host.yaml) in
          # ~/Library/Application Support/Archipelago, not next to the binary, and reseeds
          # that dir from the bundle on each launch. So validate on a throwaway copy (keeping
          # the uploaded .app intact) with its shipped templates removed, forcing the launcher
          # to regenerate them into the user dir, then confirm every world still produced one.
          USER_DIR="$HOME/Library/Application Support/Archipelago"
          rm -rf "$USER_DIR"
          APP_COPY="$RUNNER_TEMP/validate.app"
          rm -rf "$APP_COPY"
          cp -R "$APP_PATH" "$APP_COPY"
          cd "$APP_COPY/Contents/MacOS"
          ls -1 Players/Templates | grep -vx meta.yaml | sort > "$RUNNER_TEMP/setup-player-templates.txt"
          rm -R Players/Templates
          timeout 30 ./ArchipelagoLauncher "Generate Template Options" || true
          ls -1 "$USER_DIR/Players/Templates" | grep -vx meta.yaml | sort > "$RUNNER_TEMP/generated-player-templates.txt"
          cmp "$RUNNER_TEMP/setup-player-templates.txt" "$RUNNER_TEMP/generated-player-templates.txt" \
            || diff "$RUNNER_TEMP/setup-player-templates.txt" "$RUNNER_TEMP/generated-player-templates.txt"
      - name: Test Generate
        run: |
          # ArchipelagoGenerate reads player yamls from the same user dir; reuse a template
          # regenerated by the previous step as the input.
          USER_DIR="$HOME/Library/Application Support/Archipelago"
          cp "$USER_DIR/Players/Templates/VVVVVV.yaml" "$USER_DIR/Players/"
          cd "$APP_PATH/Contents/MacOS"
          timeout 30 ./ArchipelagoGenerate
      - name: Store DMG
        uses: actions/upload-artifact@v7.0.0
        with:
          name: ${{ env.DMG_BASE }}
          path: ${{ env.DMG_NAME }}
          archive: false
          if-no-files-found: error
          retention-days: 7

  build-win: # RCs and releases may still be built and signed by hand
    runs-on: windows-latest
    steps:
      # - copy code below to release.yml -
      - uses: actions/checkout@v6.0.2
      - name: Install python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: '~3.12.7'
          check-latest: true
      - name: Download run-time dependencies
        run: |
          Invoke-WebRequest -Uri https://github.com/Ijwu/Enemizer/releases/download/${Env:ENEMIZER_VERSION}/win-x64.zip -OutFile enemizer.zip
          Expand-Archive -Path enemizer.zip -DestinationPath EnemizerCLI -Force
          choco install innosetup --version=6.7.0 --allow-downgrade
      - name: Build
        run: |
          python -m pip install --upgrade pip
          python setup.py build_exe --yes
          if ( $? -eq $false ) {
            Write-Error "setup.py failed!"
            exit 1
          }
          $NAME="$(ls build | Select-String -Pattern 'exe')".Split('.',2)[1]
          $ZIP_NAME="Archipelago_$NAME.7z"
          echo "$NAME -> $ZIP_NAME"
          echo "ZIP_NAME=$ZIP_NAME" >> $Env:GITHUB_ENV
          New-Item -Path dist -ItemType Directory -Force
          cd build
          Rename-Item "exe.$NAME" Archipelago
          7z a -mx=9 -mhe=on -ms "../dist/$ZIP_NAME" Archipelago
          Rename-Item Archipelago "exe.$NAME"  # inno_setup.iss expects the original name
      - name: Build Setup
        run: |
          & "${env:ProgramFiles(x86)}\Inno Setup 6\iscc.exe" inno_setup.iss /DNO_SIGNTOOL
          if ( $? -eq $false ) {
            Write-Error "Building setup failed!"
            exit 1
          }
          $contents = Get-ChildItem -Path setups/*.exe -Force -Recurse
          $SETUP_NAME=$contents[0].Name
          echo "SETUP_NAME=$SETUP_NAME" >> $Env:GITHUB_ENV
      # - copy code above to release.yml -
      - name: Attest Build
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/attest@v4.1.0
        with:
          subject-path: |
            build/exe.*/ArchipelagoLauncher.exe
            build/exe.*/ArchipelagoLauncherDebug.exe
            build/exe.*/ArchipelagoGenerate.exe
            build/exe.*/ArchipelagoServer.exe
            dist/${{ env.ZIP_NAME }}
            setups/${{ env.SETUP_NAME }}
      - name: Check build loads expected worlds
        shell: bash
        run: |
          cd build/exe*
          mv Players/Templates/meta.yaml .
          ls -1 Players/Templates | sort > setup-player-templates.txt
          rm -R Players/Templates
          timeout 30 ./ArchipelagoLauncher "Generate Template Options" || true
          ls -1 Players/Templates | sort > generated-player-templates.txt
          cmp setup-player-templates.txt generated-player-templates.txt \
            || diff setup-player-templates.txt generated-player-templates.txt
          mv meta.yaml Players/Templates/
      - name: Test Generate
        shell: bash
        run: |
          cd build/exe*
          cp Players/Templates/VVVVVV.yaml Players/
          timeout 30 ./ArchipelagoGenerate
      - name: Store 7z
        uses: actions/upload-artifact@v7.0.0
        with:
          path: dist/${{ env.ZIP_NAME }}
          archive: false
          if-no-files-found: error
          retention-days: 7  # keep for 7 days, should be enough
      - name: Store Setup
        uses: actions/upload-artifact@v7.0.0
        with:
          path: setups/${{ env.SETUP_NAME }}
          archive: false
          if-no-files-found: error
          retention-days: 7  # keep for 7 days, should be enough

  build-ubuntu2204:
    runs-on: ubuntu-22.04
    steps:
      # - copy code below to release.yml -
      - uses: actions/checkout@v6.0.2
      - name: Install base dependencies
        run: |
          sudo apt update
          sudo apt -y install build-essential p7zip xz-utils wget libglib2.0-0
          sudo apt -y install python3-gi libgirepository1.0-dev  # should pull dependencies for gi installation below
      - name: Get a recent python
        uses: actions/setup-python@v6.2.0
        with:
          python-version: '~3.12.7'
          check-latest: true
      - name: Install build-time dependencies
        run: |
          echo "PYTHON=python3.12" >> $GITHUB_ENV
          wget -nv https://github.com/$APPIMAGE_FORK/appimagetool/releases/download/$APPIMAGETOOL_VERSION/appimagetool-x86_64.AppImage
          echo "$APPIMAGETOOL_X86_64_HASH appimagetool-x86_64.AppImage" | sha256sum -c
          wget -nv https://github.com/$APPIMAGE_FORK/type2-runtime/releases/download/$APPIMAGE_RUNTIME_VERSION/runtime-x86_64
          echo "$APPIMAGE_RUNTIME_X86_64_HASH runtime-x86_64" | sha256sum -c
          chmod a+rx appimagetool-x86_64.AppImage
          ./appimagetool-x86_64.AppImage --appimage-extract
          echo -e '#/bin/sh\n./squashfs-root/AppRun --runtime-file runtime-x86_64 "$@"' > appimagetool
          chmod a+rx appimagetool
      - name: Download run-time dependencies
        run: |
          wget -nv https://github.com/Ijwu/Enemizer/releases/download/$ENEMIZER_VERSION/ubuntu.16.04-x64.7z
          7za x -oEnemizerCLI/ ubuntu.16.04-x64.7z
      - name: Build
        run: |
          # pygobject is an optional dependency for kivy that's not in requirements
          # charset-normalizer was somehow incomplete in the github runner
          "${{ env.PYTHON }}" -m venv venv
          source venv/bin/activate
          "${{ env.PYTHON }}" -m pip install --upgrade pip "PyGObject<3.51.0" charset-normalizer
          python setup.py build_exe --yes bdist_appimage --yes
          echo -e "setup.py build output:\n `ls build`"
          echo -e "setup.py dist output:\n `ls dist`"
          cd dist && export APPIMAGE_NAME="`ls *.AppImage`" && cd ..
          export TAR_NAME="${APPIMAGE_NAME%.AppImage}.tar.gz"
          (cd build && DIR_NAME="`ls | grep exe`" && mv "$DIR_NAME" Archipelago && tar -cv Archipelago | gzip -8 > ../dist/$TAR_NAME && mv Archipelago "$DIR_NAME")
          echo "APPIMAGE_NAME=$APPIMAGE_NAME" >> $GITHUB_ENV
          echo "TAR_NAME=$TAR_NAME" >> $GITHUB_ENV
      # - copy code above to release.yml -
      - name: Attest Build
        if: ${{ github.event_name == 'workflow_dispatch' }}
        uses: actions/attest@v4.1.0
        with:
          subject-path: |
            build/exe.*/ArchipelagoLauncher
            build/exe.*/ArchipelagoGenerate
            build/exe.*/ArchipelagoServer
            dist/${{ env.APPIMAGE_NAME }}*
            dist/${{ env.TAR_NAME }}
      - name: Build Again
        run: |
          source venv/bin/activate
          python setup.py build_exe --yes
      - name: Check build loads expected worlds
        shell: bash
        run: |
          cd build/exe*
          mv Players/Templates/meta.yaml .
          ls -1 Players/Templates | sort > setup-player-templates.txt
          rm -R Players/Templates
          timeout 30 ./ArchipelagoLauncher "Generate Template Options" || true
          ls -1 Players/Templates | sort > generated-player-templates.txt
          cmp setup-player-templates.txt generated-player-templates.txt \
            || diff setup-player-templates.txt generated-player-templates.txt
          mv meta.yaml Players/Templates/
      - name: Test Generate
        shell: bash
        run: |
          cd build/exe*
          cp Players/Templates/VVVVVV.yaml Players/
          timeout 30 ./ArchipelagoGenerate
      - name: Store AppImage
        uses: actions/upload-artifact@v7.0.0
        with:
          path: dist/${{ env.APPIMAGE_NAME }}
          archive: false
          # TODO: decide if we want to also upload the zsync
          if-no-files-found: error
          retention-days: 7
      - name: Store .tar.gz
        uses: actions/upload-artifact@v7.0.0
        with:
          path: dist/${{ env.TAR_NAME }}
          archive: false
          if-no-files-found: error
          retention-days: 7
