from worlds.void_sols.test.bases import VoidSolsTestBase
from worlds.void_sols.Options import VoidSolsOptions

class TestOptions(VoidSolsTestBase):
    def test_default_options(self) -> None:
        """Test that default options are set correctly"""
        # This is implicitly tested by the base class setup, but we can verify specific defaults if needed
        pass
