# All access rules are set inline in PokepelagoWorld.create_regions() and set_rules().
# Region entrance rules: state.has("{Region} Pass", player) — set in create_regions().
# Victory rule: all Region Passes received — set in set_rules().
# Type Keys are client-side gating only (no AP access rules).
# This file is kept for structural completeness.
