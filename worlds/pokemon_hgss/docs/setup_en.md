# Pokemon HeartGold SoulSilver Setup Guide

This world is currently in early development.

At this stage, there is no playable ROM patch or emulator client.

The current version confirms that the Archipelago generator can load the world, place Johto progression items, add filler items, apply simple progression logic, read basic YAML options, include slot data, and generate a placeholder `.aphgss` output file.